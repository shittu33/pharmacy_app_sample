import 'medicine_item.dart';

class CartItem extends MedicineItem {
  CartItem(int id, String image, String title, String type, String size,
      double price)
      : super(id, image, title, type, size, price);
}
